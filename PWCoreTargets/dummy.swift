// Intentionally empty.
// This source-only target exists solely so the `PWCore` binary xcframework can
// carry its third-party dependencies (PINCache, ZipArchive/SSZipArchive), which
// a `.binaryTarget` cannot declare on its own.
